name: "retail_profitability"
version: "1.0.0"
config-version: 2

profile: "retail_profitability"

model-paths: ["models"]
test-paths: ["tests"]

models:
  retail_profitability:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
